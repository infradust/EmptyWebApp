'use strict';

/**
 * @ngdoc function
 * @name testYoApp.controller:UserCtrl
 * @description
 * # UserCtrl
 * Controller of the testYoApp
 */
angular
.module('testYoApp')
.controller('UserCtrl',['$scope','userService', function ($scope,userService) {
	$scope.userDlg = this;
	

		
}]);
